## First steps ##

#### Terminator

Install terminator from console

```
sudo apt install terminator
```

run it

```
sudo apt install terminator
```

#### Bash

Change shell to bash

```
chsh -s /bin/bash
```
